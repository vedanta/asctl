version: "2"

linters:
  settings:
    errcheck:
      # Terminal writes: a failed write to stdout/stderr is not actionable.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
